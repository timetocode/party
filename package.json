{
  "name": "party",
  "version": "0.5.0",
  "description": "websocket party service for html5 games",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npx nodemon ./example/index.js\" \"npx parcel ./example/index.html\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alex // timetocode",
  "license": "Apache 2.0",
  "dependencies": {
    "@clusterws/cws": "^0.15.0",
    "nanoid": "^2.0.3"
  },
  "devDependencies": {
    "concurrently": "^4.1.1",
    "nodemon": "^1.19.1",
    "parcel-bundler": "^1.12.3"
  }
}
